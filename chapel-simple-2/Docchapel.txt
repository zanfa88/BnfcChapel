The Language chapel
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of chapel==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in chapel are the following:
  | ``bool`` | ``break`` | ``complex`` | ``const``
  | ``continue`` | ``do`` | ``false`` | ``for``
  | ``function`` | ``if`` | ``imag`` | ``in``
  | ``int`` | ``readInt`` | ``readReal`` | ``real``
  | ``string`` | ``then`` | ``true`` | ``uint``
  | ``var`` | ``while`` | ``writeInt`` | ``writeReal``

The symbols used in chapel are the following:
  | ; | [ | ] | #
  | || | && | == | !=
  | <= | >= | < | >
  | .. | + | - | |
  | ^ | & | * | /
  | % | ( | ) | =
  | { | } | : | ,

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of chapel==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Boolean// | -> | ``true`` 
  |  |  **|**  | ``false`` 
  | //Program// | -> | //[Stmt]// 
  | //Stmt// | -> | //LExpr// //Assignment_op// //RExpr// ``;`` 
  |  |  **|**  | //StmtCondition// 
  |  |  **|**  | //StmtWhile// 
  |  |  **|**  | //StmtDo// 
  |  |  **|**  | //StmtFor// 
  |  |  **|**  | //StmtJump// ``;`` 
  |  |  **|**  | //StmtWrite// ``;`` 
  |  |  **|**  | //StmtRead// ``;`` 
  |  |  **|**  | //StmtVar// ``;`` 
  |  |  **|**  | //DefFunc// 
  |  |  **|**  | //CallFunc// 
  | //[Stmt]// | -> | //Stmt// 
  |  |  **|**  | //Stmt// //[Stmt]// 
  |  |  **|**  | **eps**
  |  |  **|**  | //Stmt// //[Stmt]// 
  | //LExpr// | -> | //Ident// 
  |  |  **|**  | //LExpr// ``[`` //RExpr13// ``]`` 
  | //RExpr// | -> | //RExpr// ``#`` //RExpr2// 
  |  |  **|**  | //RExpr1// 
  | //RExpr2// | -> | //RExpr2// ``||`` //RExpr3// 
  |  |  **|**  | //RExpr3// 
  | //RExpr3// | -> | //RExpr3// ``&&`` //RExpr4// 
  |  |  **|**  | //RExpr4// 
  | //RExpr4// | -> | //RExpr4// ``==`` //RExpr5// 
  |  |  **|**  | //RExpr4// ``!=`` //RExpr5// 
  |  |  **|**  | //RExpr5// 
  | //RExpr5// | -> | //RExpr5// ``<=`` //RExpr6// 
  |  |  **|**  | //RExpr5// ``>=`` //RExpr6// 
  |  |  **|**  | //RExpr5// ``<`` //RExpr6// 
  |  |  **|**  | //RExpr5// ``>`` //RExpr6// 
  |  |  **|**  | //RExpr6// 
  | //RExpr6// | -> | //RExpr6// ``..`` //RExpr7// 
  |  |  **|**  | //RExpr7// 
  | //RExpr7// | -> | //RExpr7// ``+`` //RExpr8// 
  |  |  **|**  | //RExpr7// ``-`` //RExpr8// 
  |  |  **|**  | //RExpr8// 
  | //RExpr8// | -> | //RExpr8// ``|`` //RExpr9// 
  |  |  **|**  | //RExpr9// 
  | //RExpr9// | -> | //RExpr9// ``^`` //RExpr10// 
  |  |  **|**  | //RExpr10// 
  | //RExpr10// | -> | //RExpr10// ``&`` //RExpr11// 
  |  |  **|**  | //RExpr11// 
  | //RExpr11// | -> | ``+`` //RExpr11// 
  |  |  **|**  | ``-`` //RExpr11// 
  |  |  **|**  | //RExpr12// 
  | //RExpr12// | -> | //RExpr12// ``*`` //RExpr13// 
  |  |  **|**  | //RExpr12// ``/`` //RExpr13// 
  |  |  **|**  | //RExpr12// ``%`` //RExpr13// 
  |  |  **|**  | //RExpr13// 
  | //RExpr13// | -> | //Constant// 
  |  |  **|**  | //RExpr14// 
  | //RExpr14// | -> | //LExpr// 
  |  |  **|**  | ``(`` //RExpr// ``)`` 
  | //RExpr1// | -> | //RExpr2// 
  | //Assignment_op// | -> | ``=`` 
  | //StmtWrite// | -> | ``writeInt`` ``(`` //Integer// ``)`` 
  |  |  **|**  | ``writeReal`` ``(`` //Double// ``)`` 
  | //StmtRead// | -> | ``readInt`` ``(`` //Integer// ``)`` 
  |  |  **|**  | ``readReal`` ``(`` //Double// ``)`` 
  | //StmtCondition// | -> | ``if`` //RExpr// ``then`` //Stmt// 
  |  |  **|**  | ``if`` ``(`` //RExpr// ``)`` ``{`` //[Stmt]// ``}`` 
  | //StmtWhile// | -> | ``while`` //RExpr// ``do`` //Stmt// 
  | //StmtDo// | -> | ``do`` ``{`` //[Stmt]// ``}`` ``while`` //RExpr// ``;`` 
  | //StmtFor// | -> | ``for`` //RExpr// ``in`` //Aggr// ``do`` ``{`` //[Stmt]// ``}`` 
  | //Aggr// | -> | //Constant// ``..`` //Constant// 
  | //StmtJump// | -> | ``break`` 
  |  |  **|**  | ``continue`` 
  | //Param// | -> | //RExpr13// 
  |  |  **|**  | ``*`` //RExpr// 
  | //Constant// | -> | //Integer// 
  | //StmtVar// | -> | ``var`` //[BlockVar]// 
  |  |  **|**  | ``const`` //Ident// ``:`` //TypeSpec// ``=`` //RExpr// 
  | //BlockVar// | -> | //Ident// ``:`` //TypeSpec// ``=`` //RExpr// 
  | //DefFunc// | -> | ``function`` //Ident// ``(`` //[Arg]// ``)`` ``{`` //[Stmt]// ``}`` 
  | //CallFunc// | -> | //Ident// ``(`` //[RExpr]// ``)`` ``;`` 
  | //Arg// | -> | //Ident// ``:`` //TypeSpec// 
  | //TypeSpec// | -> | //BasicType// 
  | //BasicType// | -> | ``bool`` 
  |  |  **|**  | ``uint`` 
  |  |  **|**  | ``int`` 
  |  |  **|**  | ``real`` 
  |  |  **|**  | ``imag`` 
  |  |  **|**  | ``complex`` 
  |  |  **|**  | ``string`` 
  | //[RExpr]// | -> | **eps**
  |  |  **|**  | //RExpr// 
  |  |  **|**  | //RExpr// ``,`` //[RExpr]// 
  | //[BlockVar]// | -> | **eps**
  |  |  **|**  | //BlockVar// 
  |  |  **|**  | //BlockVar// ``,`` //[BlockVar]// 
  | //[Arg]// | -> | **eps**
  |  |  **|**  | //Arg// 
  |  |  **|**  | //Arg// ``,`` //[Arg]// 


